
int f(int x) {
  return x * x;
}

int main() {
  int x = f(1);
  int y = 3 + x;
  while (y > 0) {
    y = 3 + x;
    boolean xd = true;
    if (xd) {
      y = 3 + x;
    }
    else {
      y = 3 + x;
    }
    x--;
  }
  printInt(y);
  return 0;
}
