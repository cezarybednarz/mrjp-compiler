

string add(string a, string b, boolean reverse) {
  if (!reverse) {
    return a + b;
  }
  else {
    return b + a;
  }
}

int main() {
  string a = "hello";
  string b = "world";
  printString(add(a, b, true));
  return 0;
}
